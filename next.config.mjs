/** @type {import('next').NextConfig} */
export default {
  // The page is a client component that fetches its data at runtime, so the
  // whole site can be exported as static files.
  output: "export",
};
